# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - T-node-web-app

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://dockerregistryt432163.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_ba073145ef5f47928045287ac411a493 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_37e2d6b36c1f4feb99209937e3451333 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: dockerregistryt432163.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_ba073145ef5f47928045287ac411a493 }}/my-nodejs-app:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'T-node-web-app'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_10bbc1bfae2e48819a08a46880ee07e8 }}
        images: 'dockerregistryt432163.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_ba073145ef5f47928045287ac411a493 }}/my-nodejs-app:${{ github.sha }}'